# Copy this file to .env and fill in your own values.
# .env is gitignored and must never be committed.

# Used by docker-compose.yml to initialize PostgreSQL
POSTGRES_USER=
POSTGRES_PASSWORD=

# Read by Spring Boot to connect to PostgreSQL
# (export these in your shell before running ./mvnw spring-boot:run)
DB_USER=
DB_PASSWORD=
